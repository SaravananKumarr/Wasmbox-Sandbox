services:
  api:
    build:
      context: ./apps/api
    ports:
      - "4000:4000"
    environment:
      PORT: 4000
  web:
    build:
      context: ./apps/web
    ports:
      - "3000:3000"
    environment:
      VITE_API_URL: http://localhost:4000
