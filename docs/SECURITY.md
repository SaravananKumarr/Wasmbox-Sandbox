# Security

- AST Security Scanner blocks dangerous imports (os, subprocess, socket, etc.).
- WASM sandbox provides CPU-level isolation.
- Memory limits enforced at 10MB.
- Fuel metering limits execution to 1M units.
